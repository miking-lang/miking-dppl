// The variance for this program is better for SMC than for IS

function flip(p) {
  sample(bernoulli(p))
}

if flip(0.5) then {
  weight(log(1))
  resample()
  weight(log(2))
} else {
  resample()
  weight(log(4))
  resample()
  weight(log(2))
}
